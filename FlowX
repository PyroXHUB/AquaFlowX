local ScreenGui = Instance.new("ScreenGui")
local loadingScreen = Instance.new("Frame") -- Frame for background gradient
local loadingUICorner = Instance.new("UICorner")
local titleLabel = Instance.new("TextLabel")
local option1 = Instance.new("TextButton")
local option1UICorner = Instance.new("UICorner")
local option2 = Instance.new("TextButton")
local option2UICorner = Instance.new("UICorner")
local option3 = Instance.new("TextButton")
local option3UICorner = Instance.new("UICorner")
local closeButton = Instance.new("TextButton")

-- Adding the ScreenGui to CoreGui
ScreenGui.Parent = game.CoreGui

-- Ensure UI Supports Mobile Executors
ScreenGui.IgnoreGuiInset = true
ScreenGui.ResetOnSpawn = false

-- Loading screen settings with gradient background
loadingScreen.Name = "LoadingScreen"
loadingScreen.Parent = ScreenGui
loadingScreen.BackgroundColor3 = Color3.fromRGB(0, 0, 0) -- Black background
loadingScreen.BackgroundTransparency = 0 -- Fully opaque to show gradient
loadingScreen.Size = UDim2.new(0.4, 0, 0.4, 0) -- Adjusted size for mobile compatibility
loadingScreen.Position = UDim2.new(0.3, 0, 0.3, 0)

local gradient = Instance.new("UIGradient")
gradient.Color = ColorSequence.new{
    ColorSequenceKeypoint.new(0, Color3.fromRGB(0, 0, 0)), 
    ColorSequenceKeypoint.new(1, Color3.fromRGB(0, 128, 255)) -- Black to ocean blue
}
gradient.Parent = loadingScreen

loadingUICorner.Parent = loadingScreen
loadingUICorner.CornerRadius = UDim.new(0, 15)

-- Title label settings (with larger text and scaling)
titleLabel.Name = "TitleLabel"
titleLabel.Parent = loadingScreen
titleLabel.Text = "NerdyScripts📜"
titleLabel.Size = UDim2.new(1, 0, 0, 70)
titleLabel.Position = UDim2.new(0, 0, 0.05, 0)
titleLabel.Font = Enum.Font.Cartoon
titleLabel.TextSize = 36
titleLabel.TextColor3 = Color3.new(1, 1, 1)
titleLabel.TextStrokeTransparency = 0
titleLabel.TextStrokeColor3 = Color3.new(0, 0, 0)
titleLabel.BackgroundTransparency = 1

-- Option 1 settings
option1.Name = "Option1"
option1.Parent = loadingScreen
option1.Text = "Auto Farm/Auto Rank & More"
option1.Size = UDim2.new(0.9, 0, 0, 40) -- Smaller buttons for mobile compatibility
option1.Position = UDim2.new(0.5, 0, 0.4, -40)
option1.Font = Enum.Font.Cartoon
option1.TextScaled = true
option1.TextColor3 = Color3.new(1, 1, 1)
option1.TextStrokeTransparency = 0
option1.TextStrokeColor3 = Color3.new(0, 0, 0)
option1.BackgroundColor3 = Color3.fromRGB(50, 160, 240)
option1.AnchorPoint = Vector2.new(0.5, 0)
option1.BorderSizePixel = 0

option1UICorner.Parent = option1
option1UICorner.CornerRadius = UDim.new(0, 15)

-- Option 2 settings
option2.Name = "Option2"
option2.Parent = loadingScreen
option2.Text = "Huge/Enchant Duping"
option2.Size = UDim2.new(0.9, 0, 0, 40)
option2.Position = UDim2.new(0.5, 0, 0.5, -20)
option2.Font = Enum.Font.Cartoon
option2.TextScaled = true
option2.TextColor3 = Color3.new(1, 1, 1)
option2.TextStrokeTransparency = 0
option2.TextStrokeColor3 = Color3.new(0, 0, 0)
option2.BackgroundColor3 = Color3.fromRGB(50, 160, 240)
option2.AnchorPoint = Vector2.new(0.5, 0)
option2.BorderSizePixel = 0

option2UICorner.Parent = option2
option2UICorner.CornerRadius = UDim.new(0, 15)

-- Option 3 settings
option3.Name = "Option3"
option3.Parent = loadingScreen
option3.Text = "NEW Gem DUPER!"
option3.Size = UDim2.new(0.9, 0, 0, 40)
option3.Position = UDim2.new(0.5, 0, 0.6, 0)
option3.Font = Enum.Font.Cartoon
option3.TextScaled = true
option3.TextColor3 = Color3.new(1, 1, 1)
option3.TextStrokeTransparency = 0
option3.TextStrokeColor3 = Color3.new(0, 0, 0)
option3.BackgroundColor3 = Color3.fromRGB(50, 160, 240)
option3.AnchorPoint = Vector2.new(0.5, 0)
option3.BorderSizePixel = 0

option3UICorner.Parent = option3
option3UICorner.CornerRadius = UDim.new(0, 15)

-- Close button settings
closeButton.Name = "CloseButton"
closeButton.Parent = loadingScreen
closeButton.Text = "X"
closeButton.Size = UDim2.new(0, 40, 0, 40)
closeButton.Position = UDim2.new(1, -45, 0, 5)
closeButton.Font = Enum.Font.Cartoon
closeButton.TextSize = 30
closeButton.TextColor3 = Color3.new(1, 0, 0)
closeButton.TextStrokeTransparency = 0
closeButton.TextStrokeColor3 = Color3.new(0, 0, 0)
closeButton.BackgroundColor3 = Color3.new(0, 0, 0)
closeButton.BorderSizePixel = 0

-- Draggable functionality
local UIS = game:GetService("UserInputService")
local dragging
local dragInput
local dragStart
local startPos

local function update(input)
    local delta = input.Position - dragStart
    loadingScreen.Position = UDim2.new(startPos.X.Scale, startPos.X.Offset + delta.X, startPos.Y.Scale, startPos.Y.Offset + delta.Y)
end

loadingScreen.InputBegan:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseButton1 or input.UserInputType == Enum.UserInputType.Touch then
        dragging = true
        dragStart = input.Position
        startPos = loadingScreen.Position

        input.Changed:Connect(function()
            if input.UserInputState == Enum.UserInputState.End then
                dragging = false
            end
        end)
    end
end)

loadingScreen.InputChanged:Connect(function(input)
    if input.UserInputType == Enum.UserInputType.MouseMovement or input.UserInputType == Enum.UserInputType.Touch then
        dragInput = input
    end
end)

UIS.InputChanged:Connect(function(input)
    if input == dragInput and dragging then
        update(input)
    end
end)

-- Close button functionality
closeButton.MouseButton1Click:Connect(function()
    loadingScreen:Destroy()
end)
